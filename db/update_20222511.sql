-- BEGIN
--     IF (semester = 1) THEN
-- 	RETURN (year)::text || '/' || (year+1) || ' Осень';
--     ELSIF (semester = 2) THEN
--         RETURN year || '/' || (year+1)::text || ' Весна';
--     END IF;

--     RETURN '';
  
-- END